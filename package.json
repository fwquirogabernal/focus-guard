{
  "name": "focus-guard",
  "version": "1.0.0",
  "description": "Focus Guard - Website Blocker Chrome Extension",
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o popup.css --minify",
    "watch:css": "tailwindcss -i src/input.css -o popup.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
